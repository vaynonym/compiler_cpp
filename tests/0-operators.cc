int main() {
  throw throw error;
  **test = 'a';

  ++ ++ a;
  a ++ ++;
  - - a;

  //;

  char test;

  return 3 * (2 + 5 / 9) * 78 % 8 >> "hello" << lol[5 - 3 + (-3)]
    <=> "somestuff"[123%32] - 123 << "33" + (som->heh(xyz + 1)
      ? ething.huh->blub.a("hello"[23 - "hm"])
      : bla.mmm(ok));

  return a ? b ? x : y : z;
  int a = b;
  throw ++a++ += b -- + std::defvalue -= 8++ + 10 != x && y || b != 3 < 5 <= b >= 3;
}